module Rebrews { 
    export interface CookieViewModel { 
        id: string;
        email: string;
        userName: string;
        firstName: string;
        role: UserRole;
        cookieVersion: number;
    }
}